6 person high top table

attributes:

current_capacity(0)
available_capacity(6)
current_height(4.0)
server(Sue)

methods:

seat_party (current_capacity + 4.0)
add_chair (available_capacity + 1.0)
move_table (server = Mark)
put_table_on_stage (current_height + 6.0 )
